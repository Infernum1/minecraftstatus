{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Welcome to MinecraftStatus Documentation","text":"<p>MinecraftStatus is an asynchronous Python wrapper for the Minecraft Server Status API. It provides an easy-to-use interface to fetch server status, generate server cards, and more.</p>"},{"location":"#getting-started","title":"Getting Started","text":""},{"location":"#installation","title":"Installation","text":"<p>You can install the latest stable release of <code>minecraftstatus</code> using pip:</p> <pre><code>pip install -U minecraftstatus\n</code></pre> <p>To install the latest development version directly from the <code>main</code> branch, run:</p> <pre><code>pip install -U git+https://github.com/Infernum1/minecraftstatus\n</code></pre> <p>Note: Make sure you have Git installed to install from the repository.</p>"},{"location":"#examples","title":"Examples","text":""},{"location":"#fetching-server-status","title":"Fetching Server Status","text":"<p>Here\u2019s a quick example to fetch the status of a Minecraft server:</p> <pre><code>import minecraftstatus\n\nclient = minecraftstatus.MCStatus()\n\nasync def get_server_status(ip: str):\n    server = await client.get_server(ip)\n    print(f\"Server {server.host} is online with {server.online_player_count} players.\")\n</code></pre>"},{"location":"#generating-an-achievement-image","title":"Generating an Achievement Image","text":"<p>You can generate a Minecraft-style achievement image:</p> <pre><code>import minecraftstatus\n\nclient = minecraftstatus.MCStatus()\n\nasync def generate_achievement(text: str):\n    image = await client.achievement(text)\n    with open(\"achievement.png\", \"wb\") as f:\n        f.write(image.getbuffer())\n</code></pre>"},{"location":"#using-in-a-discord-bot","title":"Using in a Discord Bot","text":"<p>Here\u2019s how you can integrate <code>minecraftstatus</code> into a Discord bot:</p> <pre><code>import discord\nfrom discord.ext import commands\nimport minecraftstatus\n\nclient = minecraftstatus.MCStatus()\nbot = commands.Bot(command_prefix=\"!\")\n\n@bot.command()\nasync def achievement(ctx, *, text: str):\n    image = await client.achievement(text)\n    file = discord.File(image, \"achievement.png\")\n    await ctx.send(file=file)\n\nbot.run(\"YOUR_BOT_TOKEN\")\n</code></pre> <p>For more examples, check out the examples directory in the repository.</p>"},{"location":"#api-reference","title":"API Reference","text":"<p>Explore the full API documentation:</p> <ul> <li>Client: The main class for interacting with the API.</li> <li>Errors: Exceptions raised by the library.</li> <li>Server Status: Details about the server status response.</li> </ul>"},{"location":"#need-help","title":"Need Help?","text":"<p>If you have any questions or need assistance, feel free to reach out:</p> <ul> <li>Discord: Add <code>infernum._.</code> on Discord for direct support.</li> <li>GitHub Issues: Open an issue on the GitHub repository.</li> </ul>"},{"location":"api/client/","title":"Client","text":"<p>               Bases: <code>HTTPClient</code></p> <p>Asynchronous client for interacting with api.iapetus.me</p> Usage Example <pre><code>import asyncio\nfrom minecraftstatus import MCStatus\n\nasync def main():\n    async with MCStatus() as client:\n        status = await client.get_server(\"play.hypixel.com\")\n    print(status.host, status.port)\n\nif __name__ == \"__main__\":\n    asyncio.run(main())\n</code></pre> Source code in <code>minecraftstatus\\client.py</code> <pre><code>def __init__(self):\n    super().__init__()\n</code></pre>"},{"location":"api/client/#minecraftstatus.client.MCStatus.achievement","title":"<code>achievement(achievement_text)</code>  <code>async</code>","text":"<p>Generate an image for a Minecraft achievement.</p> <p>Parameters:</p> Name Type Description Default <code>achievement_text</code> <code>str</code> <p>The name of the achievement to display (must be 1 to 30 characters).</p> required <p>Returns:</p> Name Type Description <code>BytesIO</code> <code>BytesIO</code> <p>A byte-stream object containing the achievement image.</p> <p>Raises:</p> Type Description <code>BadTextFormation</code> <p>If the achievement text is not within the 1-30 character limit.</p> Usage Example <pre><code>async with MCStatus() as client:\n    image_stream = await client.achievement(\"Mom, get the camera!!!\")\n\n# Use image_stream as needed\n</code></pre> Achievement Image Example <p></p> Source code in <code>minecraftstatus\\client.py</code> <pre><code>async def achievement(self, achievement_text: str) -&gt; BytesIO:\n    \"\"\"\n    Generate an image for a Minecraft achievement.\n\n    Args:\n        achievement_text (str): The name of the achievement to display (must be 1 to 30 characters).\n\n    Returns:\n        BytesIO: A byte-stream object containing the achievement image.\n\n    Raises:\n        BadTextFormation: If the achievement text is not within the 1-30 character limit.\n\n    ???+ example \"Usage Example\"\n        ```python\n        async with MCStatus() as client:\n            image_stream = await client.achievement(\"Mom, get the camera!!!\")\n\n        # Use image_stream as needed\n        ```\n\n    ???+ example \"Achievement Image Example\"\n        ![Achievement Image Example](/images/achievement.png)\n\n    \"\"\"\n    if not (1 &lt;= len(achievement_text) &lt;= 30):\n        raise BadTextFormation()\n\n    endpoint = BASE_URL.format(f\"mc/image/achievement/{quote(achievement_text)}\")\n    response = await self._request(\"GET\", endpoint)\n    return BytesIO(await response.read())\n</code></pre>"},{"location":"api/client/#minecraftstatus.client.MCStatus.get_server","title":"<code>get_server(ip_address)</code>  <code>async</code>","text":"<p>Retrieve the status of a Minecraft server.</p> <p>Parameters:</p> Name Type Description Default <code>ip_address</code> <code>str</code> <p>The IP address or hostname of the Minecraft server.</p> required <p>Returns:</p> Name Type Description <code>ServerStatus</code> <code>ServerStatus</code> <p>An instance containing the server's status details.</p> <p>Raises:</p> Type Description <code>ServerNotFound</code> <p>If the server is not found or is offline.</p> Usage Example <pre><code>async with MCStatus() as client:\n    status = await client.get_server(\"play.hypixel.com\")\n    print(status.host, status.port)\n</code></pre> Source code in <code>minecraftstatus\\client.py</code> <pre><code>async def get_server(self, ip_address: str) -&gt; ServerStatus:\n    \"\"\"\n    Retrieve the status of a Minecraft server.\n\n    Args:\n        ip_address (str): The IP address or hostname of the Minecraft server.\n\n    Returns:\n        ServerStatus: An instance containing the server's status details.\n\n    Raises:\n        ServerNotFound: If the server is not found or is offline.\n\n    ???+ example \"Usage Example\"\n        ```python\n        async with MCStatus() as client:\n            status = await client.get_server(\"play.hypixel.com\")\n            print(status.host, status.port)\n        ```\n    \"\"\"\n    endpoint = BASE_URL.format(f\"mc/server/status/{quote(ip_address)}\")\n    response = await self._request(\"GET\", endpoint)\n    data = await response.json()\n\n    if not data.get(\"online\", False):\n        raise ServerNotFound(ip_address)\n\n    return ServerStatus(data)\n</code></pre>"},{"location":"api/client/#minecraftstatus.client.MCStatus.get_server_card","title":"<code>get_server_card(ip_address, custom_server_name=None)</code>  <code>async</code>","text":"<p>Generate a server card image for a Minecraft server.</p> <p>Parameters:</p> Name Type Description Default <code>ip_address</code> <code>str</code> <p>The IP address or hostname of the Minecraft server.</p> required <code>custom_server_name</code> <code>str</code> <p>Custom name to be displayed on the server card. Defaults to the provided IP address if not specified.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>BytesIO</code> <code>BytesIO</code> <p>A byte-stream object containing the server card image.</p> <p>Raises:</p> Type Description <code>BadTextFormation</code> <p>If the provided text is not between 1 and 30 characters.</p> Usage Example <pre><code>async with MCStatus() as client:\n    image_stream = await client.get_server_card(\"play.hypixel.com\", \"My Server\")\n\n# Use image_stream as needed (e.g., save to file)\n</code></pre> Server Card Example <p></p> Source code in <code>minecraftstatus\\client.py</code> <pre><code>async def get_server_card(self, ip_address: str, custom_server_name: str = None) -&gt; BytesIO:\n    \"\"\"\n    Generate a server card image for a Minecraft server.\n\n    Args:\n        ip_address (str): The IP address or hostname of the Minecraft server.\n        custom_server_name (str, optional): Custom name to be displayed on the server card.\n            Defaults to the provided IP address if not specified.\n\n    Returns:\n        BytesIO: A byte-stream object containing the server card image.\n\n    Raises:\n        BadTextFormation: If the provided text is not between 1 and 30 characters.\n\n    ???+ example \"Usage Example\"\n        ```python\n        async with MCStatus() as client:\n            image_stream = await client.get_server_card(\"play.hypixel.com\", \"My Server\")\n\n        # Use image_stream as needed (e.g., save to file)\n        ```\n\n    ???+ example \"Server Card Example\"\n        ![Server Card Example](/images/server_card.png)\n\n    \"\"\"\n    server_name = custom_server_name or ip_address\n    if not (1 &lt;= len(server_name) &lt;= 30):\n        raise BadTextFormation()\n\n    endpoint = BASE_URL.format(f\"mc/server/status/{quote(ip_address)}/image?customName={quote(server_name)}\")\n    response = await self._request(\"GET\", endpoint)\n    return BytesIO(await response.read())\n</code></pre>"},{"location":"api/client/#minecraftstatus.client.MCStatus.splash_text","title":"<code>splash_text(text)</code>  <code>async</code>","text":"<p>Generate a splash text image for Minecraft.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>The text to display (must be between 1 and 30 characters).</p> required <p>Returns:</p> Name Type Description <code>BytesIO</code> <code>BytesIO</code> <p>A byte-stream object containing the splash text image.    </p> <p>Raises:</p> Type Description <code>BadTextFormation</code> <p>If the text does not meet the required length.</p> Usage Example <pre><code>async with MCStatus() as client:\n    image_stream = await client.splash_text(text)\n\n# Use image_stream as needed\n</code></pre> Splash Text Example <p></p> Source code in <code>minecraftstatus\\client.py</code> <pre><code>async def splash_text(self, text: str) -&gt; BytesIO:\n    \"\"\"\n    Generate a splash text image for Minecraft.\n\n    Args:\n        text (str): The text to display (must be between 1 and 30 characters).\n\n    Returns:\n        BytesIO: A byte-stream object containing the splash text image.    \n\n    Raises:\n        BadTextFormation: If the text does not meet the required length.\n\n    ???+ example \"Usage Example\"\n        ```python\n        async with MCStatus() as client:\n            image_stream = await client.splash_text(text)\n\n        # Use image_stream as needed\n        ```\n\n    ???+ example \"Splash Text Example\"\n        ![Splash Text Example](/images/splash_text.png)\n    \"\"\"\n    if not (1 &lt;= len(text) &lt;= 30):\n        raise BadTextFormation()\n\n    endpoint = BASE_URL.format(f\"mc/image/splash/{quote(text)}\")\n    response = await self._request(\"GET\", endpoint)\n    return BytesIO(await response.read())\n</code></pre>"},{"location":"api/errors/","title":"Errors","text":"<p>               Bases: <code>Exception</code></p> <p>Exception raised when a server is not found or is offline.</p> <p>Attributes:</p> Name Type Description <code>address</code> <code>str</code> <p>The address of the server passed.</p> Source code in <code>minecraftstatus\\errors.py</code> <pre><code>def __init__(self, address: str):\n    self.address = address\n    super().__init__(f\"Server '{self.address}' not found or is offline.\")\n</code></pre> <p>               Bases: <code>Exception</code></p> <p>Exception raised when the characters passed are not between 1 and 30 for some endpoints.</p> Source code in <code>minecraftstatus\\errors.py</code> <pre><code>def __init__(self):\n    super().__init__(\"Character limit is between 1 and 30.\")\n</code></pre>"},{"location":"api/server_status/","title":"Server Status","text":"Source code in <code>minecraftstatus\\server_status.py</code> <pre><code>def __init__(self, data: dict) -&gt; None:\n    self.data = data\n</code></pre>"},{"location":"api/server_status/#minecraftstatus.server_status.ServerStatus.clean_motd","title":"<code>clean_motd: str</code>  <code>property</code>","text":"<p>Get the clean Message Of The Day (MOTD) of the server</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The clean MOTD (without color codes)</p>"},{"location":"api/server_status/#minecraftstatus.server_status.ServerStatus.favicon","title":"<code>favicon: BytesIO</code>  <code>property</code>","text":"<p>Get the server favicon</p> <p>Returns:</p> Name Type Description <code>BytesIO</code> <code>BytesIO</code> <p>An io.BytesIO object containing the favicon, or None if not available</p>"},{"location":"api/server_status/#minecraftstatus.server_status.ServerStatus.game_map","title":"<code>game_map: str</code>  <code>property</code>","text":"<p>Get the game map of the server</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The game map</p>"},{"location":"api/server_status/#minecraftstatus.server_status.ServerStatus.gamemode","title":"<code>gamemode: str</code>  <code>property</code>","text":"<p>Get the game mode of the server</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The game mode</p>"},{"location":"api/server_status/#minecraftstatus.server_status.ServerStatus.host","title":"<code>host: str</code>  <code>property</code>","text":"<p>Get the host name of the server</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The host name (same as the IP address)</p>"},{"location":"api/server_status/#minecraftstatus.server_status.ServerStatus.is_online","title":"<code>is_online: bool</code>  <code>property</code>","text":"<p>Check if the server is online</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the server is online, False otherwise</p>"},{"location":"api/server_status/#minecraftstatus.server_status.ServerStatus.latency","title":"<code>latency: int</code>  <code>property</code>","text":"<p>Get the latency of the server</p> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>Latency in milliseconds, -1 if the server is offline</p>"},{"location":"api/server_status/#minecraftstatus.server_status.ServerStatus.max_players","title":"<code>max_players: int</code>  <code>property</code>","text":"<p>Get the maximum players the server can accommodate</p> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>The maximum number of players</p>"},{"location":"api/server_status/#minecraftstatus.server_status.ServerStatus.motd","title":"<code>motd: str</code>  <code>property</code>","text":"<p>Get the message of the day (MOTD) of the server</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The MOTD (with color codes)</p>"},{"location":"api/server_status/#minecraftstatus.server_status.ServerStatus.online_player_count","title":"<code>online_player_count: int</code>  <code>property</code>","text":"<p>Get the online player count of the server</p> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>The number of online players</p>"},{"location":"api/server_status/#minecraftstatus.server_status.ServerStatus.online_players","title":"<code>online_players: list</code>  <code>property</code>","text":"<p>Get a list of online players on the server</p> <p>Returns:</p> Name Type Description <code>list</code> <code>list</code> <p>A list of dictionaries containing player information</p>"},{"location":"api/server_status/#minecraftstatus.server_status.ServerStatus.port","title":"<code>port: int</code>  <code>property</code>","text":"<p>Get the port the server is running on</p> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>The port number, -1 if not available</p>"},{"location":"api/server_status/#minecraftstatus.server_status.ServerStatus.version_info","title":"<code>version_info: dict</code>  <code>property</code>","text":"<p>Get the descriptive version info of the server</p> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>The version information</p>"}]}